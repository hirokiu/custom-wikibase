import assert from'node:assert/strict';import test from'node:test';import{assertGenerationTransition,generationGraphIri,validateGenerationId}from'./generation.js';
test('generation lifecycle permits only recoverable declared transitions',()=>{for(const [from,to]of[['CREATING','LOADING'],['LOADING','CATCHING_UP'],['CATCHING_UP','VALIDATING'],['VALIDATING','READY'],['READY','SERVING'],['SERVING','RETIRING'],['RETIRING','RETIRED'],['RETIRING','SERVING']])assert.equal(assertGenerationTransition(from,to),true);assert.throws(()=>assertGenerationTransition('LOADING','SERVING'),/illegal/u);assert.throws(()=>assertGenerationTransition('RETIRED','SERVING'),/illegal/u);});
test('generation and partition identifiers cannot carry backend commands',()=>{assert.equal(validateGenerationId('gen-20260819-a'),'gen-20260819-a');assert.equal(generationGraphIri('gen-20260819-a','entity:Q2'),'urn:jwb:generation:gen-20260819-a:entity:Q2');assert.throws(()=>validateGenerationId('gen-x;DROP ALL'),/invalid/u);assert.throws(()=>generationGraphIri('gen-ok','entity:Q0'),/invalid/u);});
