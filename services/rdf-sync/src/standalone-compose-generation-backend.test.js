import assert from'node:assert/strict';import test from'node:test';import{StandaloneComposeGenerationBackend}from'./standalone-compose-generation-backend.js';
for(const type of['virtuoso','fuseki-tdb2','oxigraph'])test(`${type} resolves only fixed A/B service DNS without Docker`,()=>{const value=new StandaloneComposeGenerationBackend({backendType:type,adminPassword:'0123456789abcdef'});for(const id of['gen-a','gen-b']){const d=value.descriptor(id);assert.match(d.endpoints.queryUrl,new RegExp(`^http://backend-${id.at(-1)}:`,'u'));assert.doesNotMatch(JSON.stringify(d),/127\.0\.0\.1|docker/u);}assert.throws(()=>value.descriptor('gen-c'),/SLOT/u);});
test('rejects arbitrary backend and short Virtuoso credentials',()=>{assert.throws(()=>new StandaloneComposeGenerationBackend({backendType:'custom'}),/ALLOWLISTED/u);assert.throws(()=>new StandaloneComposeGenerationBackend({backendType:'virtuoso',adminPassword:'short'}),/CREDENTIAL/u);});
