import test from'node:test';import assert from'node:assert/strict';import{runtimeObservation}from'./standalone-runtime-provider.js';
const now=Date.parse('2026-08-20T00:10:00Z');
test('CURRENT remains current while an idle source reports zero synchronization lag',()=>{const value=runtimeObservation({generation_id:'gen-b',state:'CURRENT',schema_state:'CURRENT',source_status:'HEALTHY',catchup_cursor_timestamp:'2026-08-20T00:00:00Z',catchup_cursor_rcid:9,ingestion_cursor_timestamp:'2026-08-20T00:00:00Z',ingestion_cursor_rcid:9},{now});assert.equal(value.syncState,'CURRENT');assert.deepEqual(value.freshness,{cursorTimestamp:'2026-08-20T00:00:00.000Z',sourceHeadTimestamp:'2026-08-20T00:00:00.000Z',lagSeconds:0,syncLagSeconds:0,sourceIdleSeconds:600});});
test('source head ahead of serving generation is stale',()=>{const value=runtimeObservation({generation_id:'gen-a',state:'CURRENT',schema_state:'CURRENT',source_status:'HEALTHY',catchup_cursor_timestamp:'2026-08-20T00:00:00Z',catchup_cursor_rcid:9,ingestion_cursor_timestamp:'2026-08-20T00:00:02Z',ingestion_cursor_rcid:10},{now});assert.equal(value.syncState,'STALE');assert.equal(value.freshness.syncLagSeconds,2);});
